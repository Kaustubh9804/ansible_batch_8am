---
- hosts: 172.31.43.71
  become: yes
  tasks:
      - name: i want to run linux command on node
        command: hostname -f
        register: myresult

      - name: i want to display linux command output
        debug:
           msg: "{{myresult}}" 